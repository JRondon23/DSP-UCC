
  %El archivo tiene que tener el mismo nombre que la funcion para poder ser ejecutada como funcion desde
  %otro archivo.

function seno = Seno_function(A_in, f_in, t1)
  seno = A_in*sin(2*pi*f_in*t1);
end

